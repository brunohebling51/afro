// const numeros = [1, 4, 9, 10];

// console.log(numeros[1]);


function resolver(str) {
    if ('E' || '3' || '6' || '9' || 'W' || 'M' === true) {
        console.log(str);
    } else if ('e' || '3' || '6' || '9' || 'w' || 'm' === true) {
        console.log(str);
    } else {
        return false;
    }
}
resolver();